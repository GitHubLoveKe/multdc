# 分区规模接入方案 — 1/2/3+ 节点场景设计

> 日期：2026-09-18
> 前置文档：需求整理与Plan-v2.md

---

## 一、三种网区规模概览

| 网区类型 | 节点数 | quorum | 可容忍故障数 | 自动接管 | 核心挑战 |
|---------|--------|--------|-------------|---------|---------|
| 小区域 | 1 | 1 | 0 | N/A（无 peer） | 单点故障 = 全区停摆 |
| 普通网区 | 2 | 2 | 0（数学上无法自动接管） | 需额外机制 | **本节重点** |
| 重保/大网区 | ≥ 3 | ≥ 2 | ≥ 1 | ✅ 标准 VRRP 自治 | 无特殊挑战 |

2 节点区的数学困境：quorum = floor(2/2)+1 = 2，任一节点故障后剩余 1 个节点无法达成 quorum → 标准 VRRP 模型下**无法自动接管**。这不是实现缺陷，是数学必然。

---

## 二、2 节点区的三种解决方案

### 方案 A：协调层辅助 quorum（推荐）

**核心思路**：Coordinator（或 etcd）作为"虚拟第三票"，只在 2 节点区参与投票。

```
正常状态（L0）：
  Node-A ←──peer 心跳──→ Node-B
       ↘                ↗
        Coordinator（观察，不干预）

Node-A 故障：
  Node-B 检测：peer 心跳丢失 + 向 Coordinator 确认
  Coordinator 判定：A 的 session 已过期 / Coordinator 自己也探不到 A
  → 形成 "Node-B + Coordinator = 2/3 多数派"
  → Node-B 可安全接管 A 的 slot
  → 不存在脑裂风险（A 已失联，不可能同时认领）

Coordinator 也故障（L2/L3）：
  → 退化为"需双方一致"，不自动接管
  → 继续采集各自已有 slot，数据不断
  → 告警，等 Coordinator 恢复后收敛
```

**为什么安全**：2 节点区中 1 个挂了，剩下的那个 + Coordinator 构成多数派。挂掉的节点不可能同时去认领 slot（它已经失联了），所以不存在双主。如果 Coordinator 也挂了，那就冻结不动——这是安全的。

**接管流程**：
```
① Node-B 检测到 Node-A peer 心跳丢失（T_down ≈ 15s）
② Node-B 向 Coordinator 查询 Node-A 状态
③ Coordinator 确认 A 不可达（session 过期 / 自己的探测也失败）
④ Node-B 获得"虚拟 quorum"授权，签发新 token
⑤ Node-B 从本地快照获取 A 的 slot 清单 → 开始采集
⑥ catch-up 抓取 → 数据连续性恢复
总时延 ≈ T_down + Coordinator 确认（1~2s）≈ 17~19s
```

**归还流程（Node-A 恢复）**：
```
① Node-A 重启 → 连接 Coordinator → 发现自己 token 落后 → 自我卸载
② Node-A 进入 REGISTER → WARMING
③ 此时区内有 2 个健康节点 → 触发均等再均衡
④ Node-B 释放一半 slot 给 Node-A（滴水模式，每次 1~2 个，间隔 5s）
⑤ 均衡完成 → 两节点各持有 50% slot
```

### 方案 B：非对称接管（无需额外组件）

**核心思路**：两个节点角色不同——Primary 和 Secondary。只有 Secondary 能在 Primary 故障时接管，反向不行。

```
正常状态：
  Node-A (Primary)：持有自己的 slot + 不采 Secondary 的 slot
  Node-B (Secondary)：持有自己的 slot + 监控 A 的健康

Node-A (Primary) 故障：
  Node-B 检测到 A 失联 → 多重验证（peer 心跳 + 尝试连接 A 的管理口 + 等 T_down）
  → 确认 A 不可达 → Node-B 接管 A 的全部 slot
  → 不需要 quorum（因为只有一个可能的接管者，不存在竞争）

Node-B (Secondary) 故障：
  Node-A 检测到 B 失联 → 不接管 B 的 slot
  → B 的 slot 标记 UNOWNED → 告警
  → 等 B 恢复或人工介入
```

**为什么叫"非对称"**：A 挂了 B 能接管，B 挂了 A 不接管。这看起来不公平，但分析实际场景：
- Primary 故障 → 100% 自动恢复（Secondary 接管）
- Secondary 故障 → 0% 自动恢复（Primary 继续采自己的，Secondary 的 slot 空洞）
- 两个方向故障概率相同，所以整体自动恢复覆盖率 = 50%

**防脑裂的关键**：Primary 恢复后，如何知道 B 是否在代采？
```
Primary 恢复后的启动契约：
① 读取本地 C 类快照 → 记录自己上次持有的 slot 和 token
② 尝试连接 Coordinator → 获取当前归属视图
   ├─ 成功：发现 B 已代采自己的 slot → token 落后 → 自我卸载 → WARMING
   └─ 失败：尝试连接 B
        ├─ B 告知"我正在代采你的 slot"→ 接受 → WARMING
        └─ B 也联系不上 → 用 last-known-good 继续采自己的 slot（可能双采，接受风险）
```

**接管流程**：
```
① Secondary 检测 Primary 失联（T_down ≈ 15~17s）
② 多重验证：TCP 探测 + 应用级健康 + 等待 T_down
③ 确认 Primary 不可达 → 签发新 token（自增）
④ 从本地快照获取 Primary 的 slot → catch-up 抓取
总时延 ≈ T_down ≈ 15~17s（比方案 A 更快，不需要等 Coordinator 确认）
```

**归还流程**：
```
Primary 恢复：
① 连接 Coordinator（或直连 Secondary）→ 发现 token 落后
② 自我卸载 → REGISTER → WARMING
③ 触发再均衡 → Secondary 释放代采的 slot（滴水模式）
④ 或：Primary 和 Secondary 重新各 50%

Secondary 恢复（如果 Primary 没挂过）：
① Secondary 重新上线 → REGISTER → WARMING
② Primary 释放部分 slot → 均等分配
```

**方案 B 的优缺点**：
- 优点：不需要额外组件，实现简单，Primary 故障时接管更快
- 缺点：Secondary 故障时无法自动恢复（50% 覆盖率），且 Primary 恢复时的判定逻辑需要仔细处理

### 方案 C：引入 Witness（轻量仲裁进程）

**核心思路**：在区内或协调层部署一个不参与采集的轻量进程，只投票。

```
正常状态：
  Node-A ←──peer──→ Node-B ←──peer──→ Witness
  Node-A ←──peer──→ Witness

  quorum = floor(3/2)+1 = 2
  任意 2 个节点一致即可决策

Node-A 故障：
  Node-B + Witness = 2/3 多数派 → Node-B 接管
  与标准 3 节点区行为完全一致
```

**Witness 的要求极低**：
- 不参与采集、不持有任务清单、不做抓取
- 只需要：能收发 peer 心跳广告 + 能参与多数派投票
- 可以跑在任何一台机器上（甚至和 Coordinator 同机部署）
- 资源消耗 ≈ 一个空跑的微服务

**优点**：2 节点区的行为与 3 节点区完全一致，不需要特殊逻辑
**缺点**：多一个组件要部署和运维

---

## 三、方案对比

| 维度 | A 协调层辅助 | B 非对称接管 | C Witness |
|------|------------|------------|----------|
| 额外组件 | 无（复用 Coordinator） | 无 | 需部署 Witness |
| Primary 故障覆盖 | ✅ 自动接管 | ✅ 自动接管 | ✅ 自动接管 |
| Secondary 故障覆盖 | ✅ 自动接管 | ❌ 需人工/等恢复 | ✅ 自动接管 |
| 接管时延 | ~17~19s（+Coordinator 确认） | ~15~17s（无需确认） | ~15~17s |
| 脑裂风险 | 极低（Coordinator 裁决） | 低（需仔细处理恢复契约） | 极低（标准 quorum） |
| Coordinator 不可用时 | 退化为不接管（安全） | 不影响（不依赖 Coordinator） | Witness 独立于 Coordinator |
| 实现复杂度 | 中 | 低 | 中 |
| 运维负担 | 无额外 | 无额外 | 多一个 Witness 进程 |
| 与 3 节点区一致性 | 中（特殊逻辑） | 低（完全不同的模型） | 高（完全一致） |

**推荐**：
- 如果 Coordinator 稳定可靠 → **方案 A**（复用现有组件，无额外运维）
- 如果追求最简实现 → **方案 B**（接受 50% 覆盖率）
- 如果追求一致性 → **方案 C**（Witness 让 2 节点区和 3 节点区行为统一）

---

## 四、2 节点区维护场景详细设计

维护是最常见的计划内操作，2 节点区维护的核心挑战是：**一个节点维护时，另一个必须能承载全部负载**。

### 4.1 标准维护流程（make-before-break）

```
前提：Node-A 需要维护，Node-B 健康

① 运维发起 drain(Node-A, reason="版本升级", duration=30min)
② 审批通过 → Node-A 标记 maintenance(TTL=60min)
③ Node-A 置 DRAINING 状态：
   - 不再接受新 slot
   - 通知 Node-B："我要退出了，准备接管"
④ Node-B 确认就绪：
   - 检查自身容量是否足够承载全部 slot
   - 如果够 → 继续
   - 如果不够 → 拒绝 drain，告警"容量不足，无法维护"
⑤ 逐批迁移（make-before-break）：
   - 每批 1~2 个 slot
   - Node-B 预加载（本地已有 Manifest，只需就绪检查）
   - 就绪 → 签发新 token → Node-A 停采 → Node-B 开采
   - 重叠窗口 ≤ 1 抓取周期
   - 批间隔 5~10s
⑥ 全部迁出 → Node-A 静默期（WAL flush + 队列排空）
⑦ Node-A 置 OFFLINE → 开始维护
⑧ 维护完成 → Node-A 重启 → REGISTER → WARMING
⑨ 触发再均衡 → Node-B 释放部分 slot → 各 50%
```

### 4.2 容量护栏

2 节点区维护时的硬约束：

```
单节点容量必须 ≥ 全区总 slot 数

即：capacity(A) ≥ total_slots 且 capacity(B) ≥ total_slots

否则：当 A 维护时，B 无法承载全部 slot → 部分 slot 必须 UNOWNED
```

**实际建议**：2 节点区的每个节点都应按"能独立承载全区 100% 负载"来配置。这是 2 节点区的基本设计约束。如果做不到，就不应该做计划内维护（或接受维护期间的部分空洞）。

### 4.3 两节点同时维护

```
绝对禁止。

护栏：
- Node-A 处于 DRAINING/maintenance 时，Node-B 的 drain 请求被拒绝
- 错误信息："区内最后一个健康节点，拒绝驱离"
- 告警：P0 级
```

### 4.4 维护中发生故障（最坏场景）

```
场景：Node-A 正在 drain（已迁出一半 slot），Node-B 突然崩溃

处理：
① Node-A 检测到 B 崩溃
② 立即中止 drain 流程
③ Node-A 恢复 schedulable = true
④ Node-A 保留自己当前持有的 slot（不继续迁出）
⑤ 对于已迁到 B 但 B 崩溃的 slot → 等 T_down 后 Node-A 接管
   （此时 2 节点区只剩 A，quorum 不够 → 取决于选择的方案）
   - 方案 A：A 向 Coordinator 申请虚拟 quorum → 接管 B 的 slot
   - 方案 B：A 是 Primary → 直接接管；A 是 Secondary → 等 Coordinator 恢复
   - 方案 C：A + Witness = 多数派 → 接管
⑥ 告警 + 人工介入决策
```

---

## 五、1 节点区（小区域）设计

### 5.1 基本特征

```
- 无 peer，无心跳，无接管
- 单点故障 = 全区监控中断
- 适用于：非关键区域、边缘站点、测试环境
```

### 5.2 故障处理

```
Node-A 故障：
  → 平台检测到该区心跳全部丢失
  → 全区 slot 标记 UNOWNED
  → P0 告警
  → 无自动恢复手段
  → 需人工介入：修复节点 / 扩容为 2 节点

Node-A 恢复：
  → 从本地快照恢复 → 重新采集
  → 空洞 = 故障持续时长
```

### 5.3 降低风险的可选手段

| 手段 | 说明 | 代价 |
|------|------|------|
| 主机级 agent 兜底 | 主机上跑一个轻量 agent，在 Collector 挂时上报基础存活指标 | 功能有限，不是完整采集 |
| 快速重启 | 进程守护（systemd/supervisor），崩溃后自动重启，空洞 ≈ 重启时间 | 只能应对进程级故障 |
| 升级到 2 节点 | 如果该区业务重要性提升，扩容为 2 节点 + 方案 A/B/C | 容量成本 |

---

## 六、3+ 节点区（重保/大网区）

标准 VRRP 自治，无特殊挑战。补充几点：

### 6.1 节点数选择

| 节点数 | quorum | 可容忍故障 | 适用 |
|--------|--------|-----------|------|
| 3 | 2 | 1 | 重保网区标配 |
| 4 | 3 | 1 | 偶数不推荐（quorum 与 3 相同但多一个节点） |
| 5 | 3 | 2 | 大网区，可容忍同时 2 节点故障 |
| 7 | 4 | 3 | 超大网区 |

**建议**：优先选奇数节点（3/5/7），避免偶数区的"浪费一个节点"问题。

### 6.2 维护时的容量护栏

```
N 个节点，维护批次 B 需满足：
  (N - B) × 单节点容量 × 0.8 ≥ 全区总负载

N=3：B=1（一次只能维护 1 个）
N=5：B≤2（一次最多维护 2 个）
N=7：B≤2
```

---

## 七、混合部署的统一框架

三种规模的区共存时，需要统一的调度框架：

```
平台视角：
  Zone A (1 节点) → slot 均分给 1 个节点 → 无 HA
  Zone B (2 节点) → slot 均分给 2 个节点 → 协调层辅助/非对称/Witness
  Zone C (3 节点) → slot 均分给 3 个节点 → 标准 VRRP 自治

统一抽象：
  每个区注册时声明：
  {
    zone_id,
    collector_count,
    ha_mode: "none" | "coordinator_assist" | "asymmetric" | "witness" | "standard",
    quorum_policy: derived(collector_count, ha_mode)
  }

平台根据 ha_mode 决定：
  - none（1 节点）：不下发需要 HA 的任务，或标记为"无 HA 保障"
  - coordinator_assist / asymmetric / witness（2 节点）：启用对应的接管策略
  - standard（≥3 节点）：标准 VRRP 自治
```

### 各区接入 Checklist

| 步骤 | 1 节点区 | 2 节点区 | 3+ 节点区 |
|------|---------|---------|----------|
| 网区注册 | ✅ | ✅ | ✅ |
| 选择 HA 模式 | N/A | A/B/C 三选一 | 标准 VRRP |
| 协调层连接 | 基础 session | 完整（辅助 quorum 或 fencing） | 完整 |
| peer 互探 | 无 | 1 条连线 | mesh/部分 mesh |
| 快照 | A+B+C 三类 | A+B+C 三类 | A+B+C 三类 |
| 容量规划 | 单节点 ≥ 100% 总负载 | 每节点 ≥ 100% 总负载 | 每节点 ≥ 50% 总负载（N=3） |
| 告警规则 | UNOWNED 即 P0 | UNOWNED 即 P0 + 单节点降级告警 | UNOWNED 按 slot 比例告警 |

---

## 八、2 节点区方案选择建议

| 你的情况 | 推荐方案 |
|---------|---------|
| Coordinator 稳定、已有 HA | **方案 A**（协调层辅助） |
| 不想加任何额外组件 | **方案 B**（非对称，接受 50% 覆盖） |
| 追求 2 节点和 3 节点行为一致 | **方案 C**（Witness） |
| 2 节点区数量很多（>10） | **方案 C**（Witness 统一模型，运维更简单） |
| 2 节点区只有几个 | **方案 A**（复用 Coordinator，不额外部署） |

---

## 九、需要你确认的问题

1. **2 节点区选哪个方案？** A / B / C？
2. **1 节点区是否需要兜底采集？** 还是接受完全无 HA？
3. **2 节点区每节点是否按 100% 总负载配置？** 这决定了维护期间能否承载全部 slot。
4. **是否存在从 2 节点扩容到 3 节点的路径？** 如果选方案 B，后续升级到 3 节点时模型切换是否可接受？
