# 多网区 Prometheus 采集架构 — 需求整理与 Plan（v2）

> 去除加权均衡后的简化版本
> 日期：2026-09-18

---

## 一、需求整理

### 1.1 原始需求清单

| # | 需求 | 要点 |
|---|------|------|
| R1 | 每网区独立采集存储 | 各区 Prometheus 独立，互不干扰 |
| R2 | 平台统一关联 | 按网段自动划分网区，或客户指定网区下发任务 |
| R3 | RuleCheck 按网区路由 | 根据实例所在网区下发到对应 RC |
| R4 | 数据展示按网区路由 | 查询时根据实例定位网区取数据 |
| R5 | 区内心跳互备 | 采集器之间做心跳，对方下线时自动承接 |
| R6 | 上线均衡归还 | 节点恢复后任务逐步均衡或归还 |
| R7 | 不从 DB 直接拉取任务 | 通过协调层（ZK/etcd/Redis）获取 |
| R8 | 节点生命周期管理 | 任务驱离、心跳检测、下线接管、上线归还、优雅轮动 |
| R9 | 不要代码，要设计框架 | 优化设计框架，不要具体代码模型 |
| R10 | 租约模式 | 平台只管任务到区，不管区内哪个 Worker 承接 |
| R11 | 自适应协商 | 区内 Worker 自行协商任务分配 |
| R12 | 监控检测 | 调度过程中加入监控检测 |
| R13 | 分阶段任务类型 | 普通任务（主机/数据库）由网区规划默认或客户选择；探测任务（端口/拨测）由客户选择网区 |
| R14 | 可参考云原生设计 | 服务端维护期望，Worker 端自行执行 |
| R15 | 多方案对比 | 列出方案对比，不局限于用户设想 |

### 1.2 需求变更说明

**已去除**：原需求中"自动均衡负载，高配置节点会有更多任务或权重"。

去除后的影响：
- slot 分配从"加权分配"简化为"均等分配"
- score 认领函数去掉权重因子，简化为纯容量/负载驱动
- 再均衡目标从"加权均衡"简化为"均等均衡"（每节点持有相同数量 slot）
- 后续如需引入加权，可作为增量特性叠加，不影响核心架构

### 1.3 需求分层

```
┌────────────────────────────────────────────────────────────┐
│                    平台层 (Platform)                         │
│  · 网区管理 · 任务到区（声明式期望态）                        │
│  · 数据路由 · 全局监控 · 对账                                │
│  · 不管区内具体 Worker 分配                                  │
└──────────────────────────┬─────────────────────────────────┘
                           │ 期望态下发（低频、版本化）
┌──────────────────────────▼─────────────────────────────────┐
│                    协调层 (Coordination)                      │
│  ZK / etcd / 自研 Coordinator                                │
│  · 区内 slot 租约 · 心跳 session · 归属登记                  │
└──────────────────────────┬─────────────────────────────────┘
                           │ 区内协商（高频、本地闭环）
┌──────────────────────────▼─────────────────────────────────┐
│                    采集层 (Workers)                           │
│  每个网区多个 Collector / RC                                   │
│  · 均等分配 slot · VRRP 式互探 · 故障接管                     │
│  · 上线归还 · 优雅轮动 · 独立 Prometheus                      │
└────────────────────────────────────────────────────────────┘
```

---

## 二、待确认问题与冲突点

### Q1：去除加权后，节点配置差异如何处理？

去除"高配节点更多任务"后，如果区内存在不同配置的节点（比如一台 8C16G、一台 2C4G），均等分配 slot 会导致高配节点空闲、低配节点过载。

三种应对策略：

| 策略 | 做法 | 适用场景 |
|------|------|---------|
| **A. 区内同构** | 要求每个网区内部署相同配置的节点，不均等问题不存在 | 新建网区、标准化部署 |
| **B. 均等 + 容量上限** | slot 均等分配，但每个节点设一个"最大 slot 数"上限（静态配置），超过上限不再分配 | 配置差异不大、可接受轻度不均衡 |
| **C. 后续迭代加权** | 先按均等跑通，后续作为增量特性引入加权 | 当前不急需，但未来可能需要 |

**建议**：如果区内节点配置一致选 A；配置不一致但差异不大选 B；差异大且需要精确控制选 C。你倾向哪种？

### Q2：跨区连通模型选哪个？

| 模型 | 形态 | 适用 |
|------|------|------|
| **M1** | 跨区 ZK/etcd watch | 已有跨区 ZK 且运维成熟 |
| **M2** | 接口自检（digest 核对 + 增量拉取） | 通用场景，防火墙友好 |
| **M3** | 出向长连接 push | 有实时下发需求 |
| **M2+M3 混合**（推荐） | push 在线走加速，断开退化为自检 | 兼顾实时性与可靠性 |

这条决定通道实现和安全评审范围，需要优先确认。

### Q3：网区数量与单区规模？

- 网区 ≤ 5、单区规模大 → 协调层可用 etcd 3 节点
- 网区 > 10 或单区规模小 → 协调层用自研 Coordinator 双节点（RDS 租约选举）
- 直接影响探测拓扑（全连接 / 部分 mesh / 分组）

### Q4：每区 collector 节点数是否 ≥ 3？

2 节点区 quorum=2，任一节点故障都无法达成多数派 → 无法自动接管。三个选项：

| 选项 | 做法 | 代价 |
|------|------|------|
| A（推荐） | 部署 ≥ 3 个 collector | 容量成本 |
| B | 引入 witness 仲裁进程 | 额外运维 |
| C | 不做自动接管，人工介入 | 有空洞 |

### Q5：任务类型是否只有 scrape 和 probe？

当前设计区分两类：
- **scrape**（主机/数据库/中间件采集）：按网段自动推导网区
- **probe**（端口探测/拨测）：客户显式选择网区

是否还有其他任务类型需要区分？

### Q6：是否需要中心长期存储？

各区 remote-write 汇聚到中心统一存储？
- 做：需要样本级去重 + collector_id 标签规范 + 带宽预算
- 不做：各区独立存储，跨区分析通过联邦查询

### Q7：scrape slot 和 probe slot 是否独立管理？

同一网区内 scrape 和 probe 共存时：
- 独立 slot 池：互不干扰，独立扩缩
- 共享 slot 池：利用率高但互相影响

v0.2 倾向独立（rc 与 collector 独立分组的思路延伸），需要确认。

---

## 三、设计方案对比

### 3.1 调度模式对比

| 方案 | 核心思路 | 平台角色 | 区内角色 |
|------|---------|---------|---------|
| **A. 平台集中调度** | 平台计算 task→Worker 分配 | 实时调度器 | 被动执行 |
| **B. 纯租约竞争** | Worker 通过 lease 竞争 slot，先到先得 | 任务发布方 | 各自竞争 |
| **C. 一致性哈希** | target 哈希到节点 | 维护哈希环 | 按环执行 |
| **D. 声明式 + VRRP 自治** | 平台声明期望态，区内 VRRP 式自治 | 定义权威 | 自治执行 |
| **E. K8s Operator 复刻** | CRD + Controller reconcile | apiserver | kubelet |

### 3.2 多维度对比（去除加权后）

| 维度 | A 集中 | B 租约 | C 哈希 | D 声明+VRRP | E K8s |
|------|--------|--------|--------|------------|-------|
| 平台复杂度 | 高 | 低 | 低 | 低 | 中 |
| 区内复杂度 | 低 | 低 | 低 | 中 | 高 |
| 均等分配 | ✅ | ⚠️ 竞争不均 | ✅ 天然均等 | ✅ | ✅ |
| 故障接管速度 | 慢 | 快 | 快 | 快 | 快 |
| 防脑裂 | 依赖中心 HA | 弱 | N/A | 强 | 强 |
| 降级自治 | ❌ | ✅ | ✅ | ✅（四级） | ✅ |
| 数据连续性 | 中 | 低 | 低 | 高 | 中 |
| 实现成本 | 中 | 低 | 低 | 中高 | 高 |
| 与诉求匹配 | 低 | 中 | 低 | **最高** | 中 |

### 3.3 方案选择建议

**推荐方案 D（声明式 + VRRP 自治）**，理由：
- 满足"平台管到区、区内自适应协商"的核心诉求
- 四级降级保证任何单点故障不导致监控停摆
- 数据连续性有系统性保障（相位对齐 + catch-up）
- 去除加权后，slot 分配逻辑更简单，实现成本降低

**可选简化路径**：如果团队希望先快速跑通，阶段 1 可用方案 B（纯租约）起步，后续升级到 D。

### 3.4 云原生对标

| K8s 概念 | 本平台对应 |
|---------|-----------|
| Deployment（期望态） | Zone Manifest |
| kubelet | Collector + Zone Agent |
| Controller reconcile | 归属协商 + 再均衡 |
| Leader Election | Zone Coordinator 主/备 |
| Pod Disruption Budget | 驱离容量护栏 |
| Node taint/toleration | 节点标记（maintenance/quarantine/pin） |
| Rolling Update | 滚动升级 |
| Readiness Probe | WARMING 状态 |
| Liveness Probe | 三层探测（TCP + 应用级 + 金丝雀） |

不能照搬的三个原因：每个 Collector 需持有全区 Manifest（K8s kubelet 不持有所有 Pod 定义）；归属裁决必须分布式（跨区链路不可靠）；数据连续性是监控平台的核心诉求（K8s 不关心）。

---

## 四、核心机制设计概要

### 4.1 任务分发（去除加权后）

```
TaskSpec (逻辑任务, 中心定义)
    │ selector 展开 → 实例列表 → 按 zone 分组
    ▼
Zone Manifest (每区一份完整任务清单, 全区广播)
    │ 均等切分为 slot（数量恒定）
    ▼
Slot (调度最小单位, 每 slot 含相同数量 target)
    │ 归属映射（均等分配, 可变）
    ▼
Collector (区内执行者)
```

slot 分配规则简化为：
- 每区 slot 总数 = `max(64, collector数 × 16)`，上限 512
- 每节点目标 slot 数 = `slot总数 / 健康节点数`（取整）
- 余数 slot 按节点 ID 哈希确定性分配（避免每次分配结果不同）

### 4.2 心跳与故障检测

双层心跳：
- **第一层**：Worker ↔ 协调层（存活 session，15s 超时标记 suspected，30s 标记 dead）
- **第二层**：Worker ↔ Worker 直连（VRRP 式互探，3s 周期，连续 3 次超时触发接管准备）

三层探测：
- L-a 存活：TCP/HTTP
- L-b 应用健康：抓取成功率、TSDB 可写
- L-c 金丝雀：真实抓取验证

### 4.3 接管流程

```
Worker-B 检测到 Worker-A 下线
  → 多数派确认（quorum = floor(N/2)+1）
  → 确定性认领（score = 剩余容量 − 当前负载 − 近期接管次数）
  → 分批接管（每批 N 个，间隔 T 秒）
  → 更新归属
```

### 4.4 上线归还

```
Worker-A 恢复上线
  → 注册，状态 recovering
  → 渐进式接管：其他节点按"滴水模式"释放 slot
  → 每次释放 1~2 个 slot，间隔 5s
  → 均衡后标记 active
```

### 4.5 节点状态机

```
REGISTER → WARMING → HEALTHY
                         ↓ (错过心跳 + 多数派确认)
                      SUSPECT → EXPIRED → FENCED → REGISTER/WARMING
                         ↓
HEALTHY → DRAINING → OFFLINE
                         ↓ (异常行为)
                     QUARANTINED
```

### 4.6 四级降级

| 级别 | 触发 | 权威 | 冻结 |
|------|------|------|------|
| L0 正常 | 全部可用 | 中心 + Coordinator + peer | — |
| L1 中心不可达 | 跨区中断 | 区内 Coordinator | 任务定义变更 |
| L2 Coordinator 不可用 | 主节点故障 | 备节点 | 迁移类动作 |
| L3 协调器全失 | 主备皆失 | Collector 集群（纯分布式） | 再均衡、配置变更 |

---

## 五、分阶段实施 Plan

### Phase 0 · 决策收敛（1~2 周）

| 交付物 | 说明 |
|--------|------|
| 跨区连通模型 | M2+M3 混合 or 其他 |
| 网区规划 | 数量、规模、节点数 |
| 存储选型 | etcd / ZK / 自研 |
| Q1 策略确认 | 区内同构 / 均等+上限 / 后续迭代加权 |
| 任务类型清单 | scrape + probe，是否还有其他 |
| 中心长期存储 | 做 or 不做 |
| slot 独立性 | scrape/probe 独立 or 共享 |

### Phase 1 · 骨架与正确性（5~7 周）

| 模块 | 工作内容 | 周数 |
|------|---------|------|
| 中心控制面 | 实例台账、网段映射、TaskSpec/RuleSpec 版本化、Zone Manifest 生成 | 2 |
| 任务类型区分 | scrape 自动推导网区，probe 客户指定 | 0.5 |
| 跨区通道 | M2 接口自检（digest + delta） | 1 |
| 本地快照 | 三类快照 + 原子写入 + hash 校验 | 1 |
| 归属与 token | 均等 slot + 复合 token fencing + 归属注册表 | 1 |
| 节点状态机 | REGISTER → WARMING → HEALTHY → SUSPECT → EXPIRED → FENCED | 1 |
| 基础查询路由 | 查询网关 + Zone Query Proxy | 0.5 |

**验收**：
- kill -9 单节点，≤ 20s 接管
- 复活不双主
- 中心停机 30min，各区采集照常
- 时点归属查询可答

### Phase 2 · VRRP 互探与自治（4~6 周）

| 模块 | 工作内容 | 周数 |
|------|---------|------|
| peer 互探 | mesh 拓扑 + 三层探测 | 2 |
| 观测矩阵 | 双向上报 + 交叉验证 | 1 |
| 多数派投票 | quorum 判定 + 确定性认领 + skew | 1.5 |
| 四级降级 | L0→L3 完整实现 | 1 |
| 驱离与隔离 | drain/quarantine + 审批 + TTL | 1 |
| 自监控 | 关键指标接入 | 0.5 |

**验收**：
- Coordinator 主备停机，区内仍能接管（L3）
- 单向网络故障不误接管
- 节点谎报不引发风暴
- 滚动升级无 UNOWNED slot

### Phase 3 · 连续性与优化（3~5 周）

| 模块 | 工作内容 | 周数 |
|------|---------|------|
| 相位对齐 | scrape_offset 确定性推导 | 1 |
| catch-up 抓取 | 认领后立即带外抓取 | 0.5 |
| 查询侧去重 | (series, timestamp) 去重 + 空洞标注 | 1 |
| M3 长连接 | push + 退化 M2 | 1 |
| 均等再均衡 | 三重抑制（迟滞+冷却+渐进） | 1 |

**验收**：
- 崩溃空洞 ≤ 1 抓取周期
- 优雅驱离空洞 ≈ 0
- 扩缩容后 30min 内收敛

### Phase 4 · 高级特性（持续）

| 模块 | 优先级 |
|------|--------|
| 网区调整双写窗口 | 按需 |
| WAL 回填 | 低 |
| 连接预热 | 低 |
| 中心长期存储 | 按需 |
| rc 告警去重 | 中 |
| （可选）加权均衡迭代 | 按需 |

---

## 六、从简到繁的迭代路径

```
阶段 1 简化版              阶段 2 完整版              阶段 3 优化版
────────────               ────────────              ────────────
纯租约竞争            →    均等 slot + VRRP 自治  →   连续性与优化
· 先到先得               · 确定性认领               · 相位对齐
· 基础心跳               · 三层探测                 · catch-up
· 中心下发即生效          · 四级降级                 · 均等再均衡
                         · 数据连续性保障            · simulate 预演
```

---

## 七、风险清单

| # | 风险 | 缓解 |
|---|------|------|
| R1 | 脑裂双主 | 复合 token + 三层防护 + 启动契约 |
| R2 | 错误观测引发接管风暴 | 多数派投票 + probe_untrusted 识别 |
| R3 | 再均衡风暴 | 迟滞+冷却+渐进 |
| R4 | 接管雪崩 | 容量准入 + WARMING + catch-up 限流 |
| R5 | 灰故障静默漏采 | 三层探测（应用级+金丝雀） |
| R6 | 2 节点区无法多数派 | ≥3 节点 / witness / 显式接受风险 |
| R7 | 去除加权后异构节点过载 | Q1 策略（同构/上限/后续迭代） |
| R8 | 维护标记遗忘 | 强制 TTL + 责任人 + 到期提醒 |
| R9 | 半截快照 | 原子写入 + hash 校验 |
| R10 | L3 长期滞留 | degradation_level 独立告警 |
